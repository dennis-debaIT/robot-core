# Laufzeit & Secrets
.env
restore.env
firebase-credentials.json
*.db
*.db-shm
*.db-wal

# TTS-Modelle (groß, gerätespezifisch)
models/

# SSL-Zertifikate
ssl/

# Backups & temporäre Dateien
_codex_backup_*/
*.bak
*.log

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/

# Docker
.docker/

# Runtime-Flag-Dateien (werden beim Installieren erzeugt)
update.flag
reboot.flag
timezone.flag
hostname.flag
wlan.flag
ha-install.flag
components.flag
printer-start.flag
wifi-scan.json
host-ip.txt
ha_config/

# Edition-Markierung (community/plus/family) — gerätespezifisch, vom Lizenz-Check gesetzt
edition
